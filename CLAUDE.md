# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Purpose

This is a GitHub profile repository (`KimJayhyun/KimJayhyun`). The sole file is `README.md`, which GitHub renders as the public profile page for the user `KimJayhyun`.

There is no build system, test suite, or application code.

## README Structure

The README contains:
- **Blog post list** — auto-updated between `<!-- BLOG-POST-LIST:START -->` and `<!-- BLOG-POST-LIST:END -->` markers by a GitHub Actions workflow (not present in this repo).
- **GitHub Stats** — a dynamic badge from `github-readme-stats.vercel.app`.
- Several commented-out sections (tech stack, contact info, profile ideas) that can be uncommented if needed.

## Common Tasks

- **Update blog posts**: Edit the list between the `BLOG-POST-LIST` markers in `README.md`.
- **Uncomment sections**: Remove `<!--` / `-->` wrappers around the tech stack, contact, or other commented blocks to re-enable them.
